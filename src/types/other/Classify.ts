export interface Classify {
  groupName: string;
  name: string;
  code: string;
  image: string;
  quantity: number;
  increasePercent: number;
  priceAfterIncreasePercent: number;
  status: number;
}
